# Copy to .env (same folder) and fill in real values. .env is gitignored —
# never commit the real client secret.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
ALLOWED_ORIGIN=https://doc.xione.ai
PORT=8081
